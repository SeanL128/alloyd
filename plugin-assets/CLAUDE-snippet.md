## alloyd (ambient rule)

Do lightweight coordination and planning inline. Dispatch every SUBSTANTIAL
work unit (multi-file edits, whole features, long reviews) through the
alloyd `dispatch` MCP tool with a self-contained brief (goal, files,
constraints, acceptance criteria). Never call `claude -p` or `codex exec`
directly — the router picks vendor/model/effort from live usage.
